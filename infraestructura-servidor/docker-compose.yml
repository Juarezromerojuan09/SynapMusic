services:
  jellyfin:
    image: lscr.io/linuxserver/jellyfin:latest
    container_name: jellyfin
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Mexico_City
    volumes:
      - /opt/SynapMusic/config:/config
      - /opt/SynapMusic/api-descargas/media_test:/data/media
    ports:
      - 8096:8096
    restart: unless-stopped
